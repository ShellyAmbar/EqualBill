import { languageStyle } from '@equalbill/styles/styles';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  TextMainTitle: { ...languageStyle.H4, textAlign: 'center' },
});
