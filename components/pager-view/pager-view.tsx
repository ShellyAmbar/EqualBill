import { View, Text } from 'react-native';
import React from 'react';
import { PagerViewProps } from './interfaces';
const PagerView = (props: PagerViewProps) => {
  return (
    <View>
      <Text>pager-view</Text>
    </View>
  );
};

export default PagerView;
